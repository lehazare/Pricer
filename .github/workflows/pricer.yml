name: Pricer CI/CD

on:
  push:
    paths:
      - 'backend/Pricer/**'
      - '.github/workflows/pricer-ci.yml'

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    environment: OVH VPS

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ secrets.GHCR_USERNAME }}
          password: ${{ secrets.GHCR_TOKEN }}

      - name: Build & Push Pricer API
        uses: docker/build-push-action@v5
        with:
          context: ./backend/Pricer
          push: true
          tags: ghcr.io/lehazare/pricer_image:latest

      - name: Trigger Deployment
        uses: benc-uk/workflow-dispatch@v1
        with:
          workflow: deploy.yml
          token: ${{ secrets.GITHUB_TOKEN }}
